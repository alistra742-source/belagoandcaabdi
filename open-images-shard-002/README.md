# Open Images shard 002

This numbered shard contains image files organized by class folder. `manifest.csv` maps every image to its class, source URL, original URL, license, author, and title. Images originate from the Open Images validation set. Consult the manifest and retain the original license attribution before redistribution.
