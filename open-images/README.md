# Open Images class structure

This directory contains one folder for every official Open Images boxable class. See `class_map.csv` for the canonical class names. Image files are stored in numbered shards so each GitHub push remains manageable.
